import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

    container : {
        flex: 1,
    },

    camera : {
        flex: 1,
    },

    buttonView : {
        flexDirection: 'row',
    },

    button : {
        width: '100%',
        borderRadius: 0,
    }

});

export default styles;